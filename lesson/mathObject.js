let x = 3
let y = 2
let z = 1

z = Math.round(x) // arredonda o número
z = Math.floor(x) // Arredonda o número para baixo
z = Math.ceil(x) // Arrendonda o número para cima
z = Math.trunc(x) // Elimina qualquer ponto decimal
z = Math.pow(x, y) // Deixa o número ao quadrado
z = Math.sqrt(x) // Raiz quadrada
z = math.log(x) // Log
z = Math.sin(x) // seno
z = Math.cos(x) // cosseno
z = Math.tan(x) // tangente
z = Math.abs(x) // O mesmo número só que positivo
z = Math.sign(x) // Sei lá
let max = Math.max(x, y, z) // Procura o maior número entre eles
let min = Math.min(x, y, z) // Procura o menor número entre eles