/* 
let userName = "BroCode"

console.log(userName.charAt(0)) // Retorna o caractere da string baseada na posição do metodo
console.log(userName.indexOf("o")) // Retorna a primeira ocorrencia do caractere escolhida
console.log(userName.lastIndexOf("o")) // Retorna a ultima ocorrencia do caractere escolhido
console.log(userName.length) // Retorna o tamanho da string
console.log(userName.trim()) // Remove todos os espaços em branco da string
console.log(userName.toUpperCase()) // Deixa a string inteira em caixa alta
console.log(userName.toLowerCase()) // Deixa a string inteira em caixa baixa
console.log(userName.repeat(3)) // Repete a string sem espaço entre elas o número escolhido de vezes
console.log(userName.startsWith(" ")) // Retorna um valor booleano se a string começa com o caractere escolhido
console.log(userName.endsWith(" ")) // Retorna um valor booleano se a string termina com o carectere escolhido // Retorna um valor booleano se a string possui o caractere escolhido
*/

/* 
let phoneNumber = "123-456-7890"
phoneNumber = phoneNumber.replaceAll("-", "/") // Substitui todas as ocorrecias do primeiro caractere desejado pelo segundo caractere desejado
phoneNumber = phoneNumber.padStart(20, "0") // Verifica  o tamanho da string pelo primeiro metodo e substitui todos os espaços faltantes pelo segundo no começo
phoneNumber = phoneNumber.padEnd(15, "0") //Verifica  o tamanho da string pelo primeiro metodo e substitui todos os espaços faltantes pelo segundo no final
console.log(phoneNumber)
*/

